# x = 5;
x - x; x + x; x * x; x / x;
# y = 32.5;
1 / (3 + 8) * 10;
x / y; (x / y) * 32.6;
sqrt(4); sqrt(16); sqrt(15); sqrt(1);
sqrt(2*2*2*2/4); sqrt(sqrt(16));
sqrt(0); sqrt(-1);
sqrt(2 + 6.7);
k; 5*k; 3.2*k;
pow(2, 2); pow(2, 3); pow(3, 2);
pow(-1, 5);
Q;
